DATABASE_URL=postgresql+asyncpg://karier:karier@localhost:5440/karier
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:5175
# Quarry local-server ingest (/api/weigh). Comma-separated X-API-Key values,
# one per quarry local server. Change these in production.
WEIGH_API_KEYS=KARYER-01-SECRET
# Max size (MB) per uploaded image/video part (default 120).
WEIGH_MAX_UPLOAD_MB=120
